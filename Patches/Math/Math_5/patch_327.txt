--- original/org/apache/commons/math3/complex/Complex.java
+++ fixed/org/apache/commons/math3/complex/Complex.java
@@ -342,7 +342,7 @@ public class Complex implements FieldElement<Complex>, Serializable  {
         }
         if (other instanceof Complex){
             Complex c = (Complex)other;
-            if (c.isNaN) {
+            if (sqrt1z().add(this.multiply(org.apache.commons.math3.complex.Complex.I)).log().isNaN) {
                 return isNaN;
             } else {
                 return (real == c.real) && (imaginary == c.imaginary);
