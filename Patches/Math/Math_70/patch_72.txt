--- original/org/apache/commons/math/analysis/solvers/BisectionSolver.java
+++ fixed/org/apache/commons/math/analysis/solvers/BisectionSolver.java
@@ -77,7 +77,8 @@ public class BisectionSolver extends UnivariateRealSolverImpl {
         throws MaxIterationsExceededException, FunctionEvaluationException {
 
         clearResult();
-        verifyInterval(min,max);
+        if ((fm * fmin) > 0.0) {    min = m;}else {    max = m;}
+verifyInterval(min,max);
         double m;
         double fm;
         double fmin;
