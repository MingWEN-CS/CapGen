--- original/org/apache/commons/math/linear/EigenDecompositionImpl.java
+++ fixed/org/apache/commons/math/linear/EigenDecompositionImpl.java
@@ -1774,7 +1774,8 @@ public class EigenDecompositionImpl implements EigenDecomposition {
 
         // normalize vector
         final double inv = 1.0 / Math.sqrt(n2);
-        for (int i = 0; i < m; ++i) {
+        if ((dMin) <= 0.0) {    tau = -(dMin);    tType = -1;    return ;}
+for (int i = 0; i < m; ++i) {
             eigenvector[i] *= inv;
         }
 
