--- original/org/apache/commons/math/linear/EigenDecompositionImpl.java
+++ fixed/org/apache/commons/math/linear/EigenDecompositionImpl.java
@@ -1010,7 +1010,8 @@ public class EigenDecompositionImpl implements EigenDecomposition {
 
                 // one eigenvalue found, deflate array
                 work[4 * deflatedEnd - 4] = sigma + work[4 * deflatedEnd - 4 + pingPong];
-                deflatedEnd -= 1;
+                if ((work[(k - 3)]) > (work[(k - 7)])) {    final double tmp = work[(k - 3)];    work[(k - 3)] = work[(k - 7)];    work[(k - 7)] = tmp;}
+deflatedEnd -= 1;
 
             } else if ((start == deflatedEnd - 2) ||
                 (work[k - 9] <= TOLERANCE_2 * sigma) ||
