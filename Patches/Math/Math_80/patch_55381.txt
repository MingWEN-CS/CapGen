--- original/org/apache/commons/math/linear/EigenDecompositionImpl.java
+++ fixed/org/apache/commons/math/linear/EigenDecompositionImpl.java
@@ -934,7 +934,8 @@ public class EigenDecompositionImpl implements EigenDecomposition {
      */
     private void initialSplits(final int n) {
 
-        pingPong = 0;
+        if ((work[(i + 2)]) <= ((org.apache.commons.math.linear.EigenDecompositionImpl.TOLERANCE_2) * d)) {    work[(i + 2)] = -0.0;    d = work[i];}else {    d *= (work[i]) / (d + (work[(i + 2)]));}
+pingPong = 0;
         for (int k = 0; k < 2; ++k) {
 
             // apply Li's reverse test
