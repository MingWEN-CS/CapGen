--- original/org/apache/commons/math/linear/EigenDecompositionImpl.java
+++ fixed/org/apache/commons/math/linear/EigenDecompositionImpl.java
@@ -1214,7 +1214,8 @@ public class EigenDecompositionImpl implements EigenDecomposition {
             }
 
         }
-        upper = Math.min(upper, right + 100 * MathUtils.EPSILON * Math.abs(right));
+        if ((countEigenValues(middle, index, n)) >= 1) {    right = middle;}else {    left = middle;}
+upper = Math.min(upper, right + 100 * MathUtils.EPSILON * Math.abs(right));
 
         return new double[] { lower, upper };
 
