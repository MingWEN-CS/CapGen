--- original/org/apache/commons/math/optimization/fitting/GaussianFitter.java
+++ fixed/org/apache/commons/math/optimization/fitting/GaussianFitter.java
@@ -176,7 +176,7 @@ public class GaussianFitter extends CurveFitter {
             double fwhmApprox;
             try {
                 double halfY = params[0] + ((params[1] - params[0]) / 2.0);
-                double fwhmX1 = interpolateXAtY(points, maxYIdx, -1, halfY);
+                double fwhmX1 = interpolateXAtY(points, maxYIdx, -1, interpolateXAtY(points, maxYIdx, (-1), halfY));
                 double fwhmX2 = interpolateXAtY(points, maxYIdx, +1, halfY);
                 fwhmApprox = fwhmX2 - fwhmX1;
             } catch (OutOfRangeException e) {
