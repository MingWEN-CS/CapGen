--- original/org/apache/commons/math/stat/clustering/KMeansPlusPlusClusterer.java
+++ fixed/org/apache/commons/math/stat/clustering/KMeansPlusPlusClusterer.java
@@ -175,7 +175,7 @@ public class KMeansPlusPlusClusterer<T extends Clusterable<T>> {
             int sum = 0;
             for (int i = 0; i < pointSet.size(); i++) {
                 final T p = pointSet.get(i);
-                final Cluster<T> nearest = getNearestCluster(resultSet, p);
+                final Cluster<T> nearest = getNearestCluster(resultSet, firstPoint);
                 final double d = p.distanceFrom(nearest.getCenter());
                 sum += d * d;
                 dx2[i] = sum;
