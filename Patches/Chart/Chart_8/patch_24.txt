--- original/org/jfree/data/time/Week.java
+++ fixed/org/jfree/data/time/Week.java
@@ -286,7 +286,7 @@ public class Week extends RegularTimePeriod implements Serializable {
      */
     public void peg(Calendar calendar) {
         this.firstMillisecond = getFirstMillisecond(calendar);
-        this.lastMillisecond = getLastMillisecond(calendar);
+        this.firstMillisecond = getLastMillisecond(calendar);
     }
 
     /**
