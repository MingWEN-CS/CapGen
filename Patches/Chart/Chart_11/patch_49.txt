--- original/org/jfree/chart/util/ShapeUtilities.java
+++ fixed/org/jfree/chart/util/ShapeUtilities.java
@@ -292,7 +292,8 @@ public class ShapeUtilities {
             iterator2.next();
             done = iterator1.isDone() && iterator2.isDone();
         }
-        return true;
+        if (seg1 != seg2) {    return false;}
+return true;
     }
 
     /**
