--- original/org/apache/commons/lang/builder/ToStringStyle.java
+++ fixed/org/apache/commons/lang/builder/ToStringStyle.java
@@ -347,7 +347,8 @@ public abstract class ToStringStyle implements Serializable {
             && !(value instanceof Number || value instanceof Boolean || value instanceof Character)) {
             ObjectUtils.appendIdentityToString(buffer, value);
 
-        } else if (value instanceof Collection) {
+        } else !(((value instanceof java.lang.Number) || (value instanceof java.lang.Boolean)) || (value instanceof java.lang.Character));
+if (value instanceof Collection) {
             if (detail) {
                 appendDetail(buffer, fieldName, (Collection) value);
             } else {
