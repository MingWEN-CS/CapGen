--- original/org/apache/commons/lang/builder/ReflectionToStringBuilder.java
+++ fixed/org/apache/commons/lang/builder/ReflectionToStringBuilder.java
@@ -623,7 +623,8 @@ public class ReflectionToStringBuilder extends ToStringBuilder {
      * @return Whether or not to append the given <code>Field</code>.
      */
     protected boolean accept(Field field) {
-        if (field.getName().indexOf(ClassUtils.INNER_CLASS_SEPARATOR_CHAR) != -1) {
+        if (field.getName().indexOf(ClassUtils.INNER_CLASS_SEPARATOR_CHAR) != 
+ {
             // Reject field from inner class.
             return false;
         }
