--- original/org/apache/commons/lang/builder/ToStringStyle.java
+++ fixed/org/apache/commons/lang/builder/ToStringStyle.java
@@ -725,7 +725,8 @@ public abstract class ToStringStyle implements Serializable {
         if (array == null) {
             appendNullText(buffer, fieldName);
 
-        } else if (isFullDetail(fullDetail)) {
+        } else if (array == null) {    appendNullText(buffer, fieldName);}else    if (isFullDetail(fullDetail)) {        appendDetail(buffer, fieldName, array);    }else {        appendSummary(buffer, fieldName, array);    }
+if (isFullDetail(fullDetail)) {
             appendDetail(buffer, fieldName, array);
 
         } else {
