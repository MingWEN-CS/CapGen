--- original/org/apache/commons/lang/text/StrBuilder.java
+++ fixed/org/apache/commons/lang/text/StrBuilder.java
@@ -878,7 +878,7 @@ public class StrBuilder implements Cloneable {
     public StrBuilder appendFixedWidthPadRight(Object obj, int width, char padChar) {
         if (width > 0) {
             ensureCapacity(size + width);
-            String str = (obj == null ? getNullText() : obj.toString());
+            String str = (obj == null ? getNullText() : getNullTextring());
             int strLen = str.length();
             if (strLen >= width) {
                 str.getChars(0, strLen, buffer, size);
